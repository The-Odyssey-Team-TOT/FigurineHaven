<nav class="p-4">
  <div class="container mx-auto flex items-center justify-between">
    <a href="/" class="flex items-center">
      <%= image_tag "favicon.png", class: "h-8 w-8 mr-2  rounded-md" %>
      <span class="text-black">FigurineHaven</span>
    </a>

    <div class="flex items-center">
      <%= link_to 'Products', shops_path, class: 'text-black hover:text-white px-3 py-2 rounded-md text-sm font-medium', data: { turbo: false } %>
      <%= link_to 'My orders', orders_path, class: 'text-black hover:text-white px-3 py-2 rounded-md text-sm font-medium', data: { turbo: false } %>
      <%# <%= link_to 'Add a new Product', new_shop_path, id: 'nav-btn', class: 'ml-4 inline-block text-white font-bold py-2 px-4 rounded-lg' %>

      <% if user_signed_in? %>
        <%= link_to 'Cart', cart_path, class: 'text-black hover:text-white px-3 py-2 rounded-md text-sm font-medium', data: { turbo: false } %>
        <%= button_to 'Log out', destroy_user_session_path, method: :delete, class: 'ml-4 inline-block text-black font-bold py-2 px-4 rounded-lg' %>
      <% else %>
        <%= link_to 'Log in', new_user_session_path, class: 'text-black hover:text-white px-3 py-2 rounded-md text-sm font-medium' %>
        <%= link_to 'Sign up', new_user_registration_path, class: 'ml-4 inline-block text-black font-bold py-2 px-4 rounded-lg' %>
      <% end %>
    </div>
  </div>
</nav>


<style>

nav {
  background-color: #ffeb02;
}

</style>
